#!/bin/sh

set -e
version="BUILDTAG"

case "$1" in
    upgrade|remove)
        cd /usr/src/vrouter-$version/
        dkms remove -m vrouter -v $version --all
        make clean
        cd /usr/src
        rm -rf vrouter-$version
        ;;
    *)
        ;;
esac
