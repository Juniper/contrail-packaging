#!/bin/bash

if [ -f /opt/contrail/.contrail_openstack_packages ]; then
    echo -e "\nYou may need to run /opt/contrail/contrail_openstack_packages/remove.sh before uninstalling the package..."
    exit 1
fi
