# ****************************************************************
# CAUTION: ANY CHANGES TO DEFAULT APPLIES TO ALL SECTIONS
#          IF NOT OVERWRITTEN LOCALLY
# ****************************************************************
# Usage:
# [build target name]
# pkgs =        # list of packages generated by the make target
#
# pkg_pattern = # pattern to match packages. would be used
#               # packager to pick packages from given directory
#
# builtloc =    # location where packages are copied after built
#
# makeloc =     # location where make command has to issued
#               # these path are taken relative to git local repo
# *****************************************************************

[DEFAULT]
pkgs        = 
pkg_pattern = {pkg}_[0-9]*.deb
builtloc    = build/debian/
target      = %(__name__)s
makeloc     =

[openstack-quantum-deb]
pkgs      = quantum-server, quantum-common, python-quantum, quantum-plugin-contrail
makeloc   = tools/packaging/openstack/debian

[openstack-neutron-deb]
pkgs      = 
makeloc   = tools/packaging/openstack/debian

[contrail-libs-deb]
pkgs      = contrail-libs
makeloc   = tools/packaging/common/debian

[contrail-analytics-venv-deb]
pkgs      = contrail-analytics-venv
makeloc   = tools/packaging/common/debian

[contrail-analytics-deb]
pkgs      = contrail-analytics
makeloc   = tools/packaging/common/debian

[contrail-dns-deb]
pkgs      = contrail-dns
makeloc   = tools/packaging/common/debian

[contrail-vrouter-deb]
pkgs      = contrail-vrouter
makeloc   = tools/packaging/common/debian

[contrail-vrouter-init-deb]
pkgs      = contrail-vrouter-init
makeloc   = tools/packaging/common/debian

[contrail-config-venv-deb]
pkgs      = contrail-config-venv
makeloc   = tools/packaging/common/debian

[contrail-config-deb]
pkgs      = contrail-config
makeloc   = tools/packaging/common/debian

[contrail-config-extension-deb]
pkgs      = contrail-config-extension
makeloc   = tools/packaging/common/debian

[contrail-control-deb]
pkgs      = contrail-control
makeloc   = tools/packaging/common/debian

[contrail-api-lib-deb]
pkgs      = contrail-api-lib
makeloc   = tools/packaging/common/debian

[contrail-nova-vif-deb]
pkgs      = contrail-nova-vif
makeloc   = tools/packaging/common/debian

[contrail-nodejs-deb]
pkgs      = contrail-nodejs
makeloc   = tools/packaging/common/debian

[contrail-webui-deb]
pkgs      = contrail-webui
makeloc   = tools/packaging/common/debian

[contrail-nodemgr-deb]
pkgs      = contrail-nodemgr
makeloc   = tools/packaging/common/debian

[contrail-setup-deb]
pkgs      = contrail-setup
makeloc   = tools/packaging/common/debian
        
[contrail-install-packages]
pkgs      = contrail-install-packages
makeloc   = tools/packaging/common/debian
target    = contrail-install-packages-deb

[thirdparty-all]
pkgs      =
makeloc   = tools/packaging/third_party/debian
target    = all

[contrail-all]
pkgs      =
makeloc   = tools/packaging/common/debian/
target    = all

[openstack-all]
pkgs      =
makeloc   = tools/packaging/openstack/debian
target    = all
